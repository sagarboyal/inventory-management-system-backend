server:
  port: ${PORT:8080}
  servlet:
    context-path: "/api"

spring:
  application:
    name: inventory-management

  profiles:
    active: dev

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG